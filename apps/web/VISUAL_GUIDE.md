# AIScan 视觉设计指南

## 🎨 设计理念

AIScan 的视觉设计遵循**现代、清新、年轻化**的原则,通过精心设计的颜色系统、排版规范和交互细节,为用户提供愉悦的阅读体验。

## 🌈 颜色应用示例

### 主色应用

**青绿色 (#2bc3c0)** 作为品牌主色,传达科技感和活力:

- ✅ 品牌标识 (Logo、标题渐变)
- ✅ 主要操作按钮
- ✅ 链接和强调文本
- ✅ 选中状态的筛选器
- ✅ 排名徽章背景

### 辅助色应用

**紫色 (#b87dff)** 作为辅助色,增添优雅气质:

- ✅ 渐变效果 (与主色组合)
- ✅ X/Twitter 分类标识
- ✅ 装饰性元素

### 中性色应用

**灰色系列** 用于构建信息层级:

- 主要文本: `#1a2530` (深灰,高对比度)
- 次要文本: `#4a5968` (中灰,说明性信息)
- 辅助文本: `#6b7a88` (浅灰,元数据)
- 边框: `#e4e9ed` (极浅灰,分隔线)
- 背景: `#f8fafb` (接近白色,主背景)

## 📐 布局结构

### 页面布局

```
┌─────────────────────────────────────┐
│  Hero 区域                           │
│  - 品牌标识 (EYEBROW + 标题)         │
│  - 副标题说明                        │
│  - 统计信息 (日期、总条目、高置信)   │
│  - 日期导航 (前一天/后一天)          │
│  - 内容筛选器 (全部/产品/技术/...)   │
├─────────────────────────────────────┤
│  Radar 概览 (2x2 网格)               │
│  ┌──────────┬──────────┐            │
│  │ Product  │  Tech    │            │
│  │ Hunt     │  HF      │            │
│  ├──────────┼──────────┤            │
│  │ Reddit   │  X/      │            │
│  │ Dev      │  Twitter │            │
│  └──────────┴──────────┘            │
├─────────────────────────────────────┤
│  内容卡片列表                        │
│  ┌─────────────────────────────┐   │
│  │ #1 [高置信] 🔥 0.856        │   │
│  │ 标题                         │   │
│  │ 摘要内容...                  │   │
│  │ [标签1] [标签2]              │   │
│  │ 元信息 · 时间 · 类型         │   │
│  │ 查看原文 →                   │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ #2 ...                       │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  Footer                              │
│  数据来源说明                        │
└─────────────────────────────────────┘
```

### 间距规范

- **页面边距**: 左右 16px (移动端) / 自动居中 (桌面端)
- **区块间距**: 24px - 48px
- **卡片间距**: 20px
- **组件内间距**: 12px - 20px
- **元素间距**: 8px - 12px

## 🎯 组件设计

### Hero 区域

**品牌标识 (EYEBROW)**
```
┌─────────────────────────────┐
│ GLOBAL AI DAILY BRIEF       │  ← 小号大写字母,青绿色
└─────────────────────────────┘
   带浅色背景和边框的胶囊形状
```

**主标题**
```
AIScan  ← 大号粗体,渐变色 (青绿→紫色)
```

**统计信息**
```
┌──────┐ ┌──────────┐ ┌──────────┐
│📅 日期│ │📊 总条目 │ │⭐ 高置信 │
└──────┘ └──────────┘ └──────────┘
  浅色背景,圆角,带边框
```

### Radar 卡片

```
┌─────────────────────────────┐
│ Product Hunt AI              │ ← 粗体标题
│ 看新产品爆发                 │ ← 灰色副标题
│                              │
│ 20 条                        │ ← 青绿色数字
│ 最新热点标题...              │ ← 灰色,单行省略
└─────────────────────────────┘
  白色背景,渐变边框,悬停上浮
  顶部有 3px 彩色条 (悬停显示)
```

**颜色标识**:
- Product Hunt: 蓝色系 (#3b82f6)
- Tech/HF: 绿色系 (#10b981)
- Community: 橙色系 (#f59e0b)
- X/Twitter: 紫色系 (#8b5cf6)

### 内容卡片

```
┌─────────────────────────────────────┐
│ ┌──┐ [高置信] 🔥 0.856 🔁 持续3天  │ ← 头部信息
│ │#1│                                │   排名徽章(渐变)
│ └──┘                                │
│                                     │
│ 这是一个很长的标题文本              │ ← 大号粗体
│                                     │
│ 这是摘要内容,最多显示三行,超出     │ ← 正文大小
│ 部分会被省略号截断...              │   灰色文本
│                                     │
│ [AI模型] [开源] [性能优化]          │ ← 标签行
│                                     │
│ [新产品爆发] 来源 · 时间 · 类型     │ ← 元信息
│                                     │
│ 查看原文 →                          │ ← 链接,悬停右移
└─────────────────────────────────────┘
  白色背景,阴影,悬停上浮 + 阴影加深
```

**置信度徽章**:
- 高置信: 绿色背景 + 深绿文字
- 中置信: 黄色背景 + 深黄文字
- 低置信: 灰色背景 + 深灰文字

### 筛选器

```
┌──────┐ ┌──────────┐ ┌──────────┐
│ 全部 │ │🚀 产品爆发│ │🔬 技术方向│ ...
└──────┘ └──────────┘ └──────────┘
  未选中: 白色背景,灰色边框
  选中: 青绿色背景,青绿色边框,粗体
```

## ✨ 交互效果

### 悬停效果

**卡片悬停**:
- 向上移动 2px
- 阴影从 `shadow-sm` 变为 `shadow-md`
- 边框颜色加深
- 过渡时间: 250ms

**按钮/链接悬停**:
- 背景色变深
- 文字颜色变化
- 过渡时间: 150ms

**Radar 卡片悬停**:
- 向上移动 4px
- 顶部彩色条显示 (opacity 0 → 1)
- 阴影加深
- 边框变为品牌色

### 点击效果

**按钮点击**:
- 向下移动 (取消 translateY)
- 背景色进一步加深

**链接点击**:
- 箭头向右移动 2px
- 间距增加

### 加载动画

**页面加载**:
- 淡入 + 向上移动 8px
- 持续时间: 250ms

**骨架屏** (如需要):
- 渐变扫光效果
- 从左到右移动
- 循环动画

## 📱 响应式设计

### 桌面端 (> 640px)

- 最大宽度: 960px,居中显示
- Radar 网格: 2x2
- 卡片: 单列,宽度自适应
- 字体: 标准大小

### 移动端 (≤ 640px)

- 全宽显示,左右 16px 边距
- Radar 网格: 1 列
- 主标题: 从 48px 缩小到 30px
- 卡片标题: 从 24px 缩小到 18px
- 摘要: 从 16px 缩小到 14px
- 筛选器: 更紧凑的间距

### 小屏手机 (≤ 480px)

- 主标题: 进一步缩小到 24px
- 卡片内边距: 从 20px 缩小到 16px
- 统计信息: 更紧凑的布局

## 🎨 视觉层次

### 信息优先级

1. **最高优先级** (吸引注意)
   - 排名徽章 (渐变背景)
   - 置信度标签 (彩色背景)
   - 卡片标题 (大号粗体)

2. **高优先级** (主要内容)
   - 摘要文本 (正文大小)
   - 分类标签 (彩色背景)
   - 热度分数

3. **中优先级** (辅助信息)
   - 标签列表
   - 数据源名称
   - 发布时间

4. **低优先级** (次要信息)
   - 内容类型 (研究者/实用)
   - Footer 说明文字

### 颜色对比度

所有文本颜色都经过精心选择,确保符合 WCAG AA 标准:

- 主要文本 vs 白色背景: 对比度 > 7:1
- 次要文本 vs 白色背景: 对比度 > 4.5:1
- 链接文本 vs 白色背景: 对比度 > 4.5:1

## 🌟 设计亮点

### 1. 渐变标题
主标题使用青绿到紫色的渐变,既现代又吸睛,体现品牌特色。

### 2. 彩色顶条
Radar 卡片悬停时显示顶部彩色条,不同数据源使用不同颜色,视觉识别度高。

### 3. 微动画
所有交互都有流畅的过渡动画,提升用户体验,但不过度使用,保持专业感。

### 4. 信息密度平衡
在有限空间内展示丰富信息,但通过合理的间距和层次,避免拥挤感。

### 5. 一致性
所有组件使用统一的设计令牌,确保视觉一致性,降低认知负担。

## 🔧 实现细节

### CSS 变量使用

所有样式都通过 CSS 变量实现,便于主题切换和维护:

```css
/* 颜色 */
color: var(--text-primary);
background: var(--surface-primary);
border-color: var(--border-default);

/* 间距 */
padding: var(--spacing-4);
gap: var(--spacing-3);

/* 圆角 */
border-radius: var(--radius-lg);

/* 阴影 */
box-shadow: var(--shadow-sm);

/* 过渡 */
transition: all var(--transition-base);
```

### 渐变实现

```css
/* 品牌渐变 */
background: linear-gradient(135deg, #2bc3c0 0%, #b87dff 100%);

/* 文字渐变 */
background: var(--gradient-brand);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
background-clip: text;

/* 背景装饰 */
background-image: 
  radial-gradient(circle at 10% 5%, #b3f0ed 0%, transparent 50%),
  radial-gradient(circle at 90% 8%, #e4ccff 0%, transparent 45%);
```

### 悬停效果实现

```css
.card {
  transition: all var(--transition-base);
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
  border-color: var(--border-strong);
}
```

## 📚 参考

- 设计系统完整文档: [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md)
- 样式文件: [`src/styles/`](src/styles/)
- 组件实现: [`src/components/digest-page.tsx`](src/components/digest-page.tsx)

---

**提示**: 本指南展示了设计系统的实际应用效果。在开发新功能时,请参考这些示例,保持视觉一致性。
